{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The OSII launcher window may use dialogs and registered launcher commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
