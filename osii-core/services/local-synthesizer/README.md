# Local extractive-preview synthesizer

Processor API v1 service `local.extractive-preview`. Run `make dev`
and open <http://localhost:8093/docs>. `POST /v1/synthesize` returns
deterministic Markdown excerpts plus source citations. It is deliberately
labelled an extractive preview, not AI-generated synthesis.
