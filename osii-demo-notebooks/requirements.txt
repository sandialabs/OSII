# Self-contained environment for the Python and Jupytext demonstrations.
# Run this file from osii-demo-notebooks:
#   python -m pip install -r requirements.txt
#
# The editable local packages are intentional: OSII is a monorepo and these
# packages are not expected to be downloaded from PyPI. The demo scripts call
# optional processor services over HTTP, so those services are not installed
# into this environment; start them separately when a demo requires one.
-e ../packages/osii-processor-sdk
-e ../ai-ready-ingest

# Notebook and conversion tools.
jupyterlab>=4,<5
ipykernel>=6,<7
jupytext>=1.17,<2
